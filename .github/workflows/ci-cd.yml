name: CI / CD Pipeline

on:
  push:
    branches:
      - main
jobs:
  build_and_test:
    runs-on: ubuntu-latest

    env:
      Solution: './TECH_CHALLENGE.sln'
      Main: './ContatosAPI/ContatosAPI.csproj'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.x'

    - name: Restore dependencies
      run: dotnet restore ${{ env.Solution }}

    - name: Build main project
      run: dotnet build ${{ env.Main }} --no-restore --configuration Release

    - name: List directory contents
      run: ls -la ./TECH_CHALLENGE.Tests/

    - name: Run tests
      run: dotnet test './TECH_CHALLENGE.Tests/TECH_CHALLENGE.Tests.csproj' --no-build --verbosity detailed --configuration Release